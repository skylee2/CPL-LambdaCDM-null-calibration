# Manuscript support

`reported_values.md` is a compact, human-readable audit target transcribed from the final PDU manuscript. The machine-readable realization products are in `data/`, and `scripts/audit_scientific_consistency.py` checks the principal manuscript landmarks directly from those products.

The manuscript source and PDF are not duplicated in this code/data package. The final artwork PDFs are included in `figures/`.
